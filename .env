# Frontend Configuration
# These will be updated after backend deployment with the actual URLs
VITE_API_URL=${VITE_API_URL:-https://chess-ai-backend.onrender.com}
VITE_SOCKET_URL=${VITE_SOCKET_URL:-https://chess-ai-backend.onrender.com}

# Build Configuration
NODE_ENV=production
